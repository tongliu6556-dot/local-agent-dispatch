Fix the implementation of `clamp` in `buggy_math.py` with the smallest clear
change. Do not weaken, delete, or rewrite the tests. Run the configured test
command and leave the working tree with all tests passing.
